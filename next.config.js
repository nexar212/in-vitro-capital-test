/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['randomuser.me'],
  },
  // Asegúrate de que no haya configuraciones que interfieran con los archivos estáticos
}

module.exports = nextConfig 